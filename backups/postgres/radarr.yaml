apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: radarr-weekly-backup
  namespace: seedbox
spec:
  immediate: true
  schedule: "@weekly"
  backupOwnerReference: self
  cluster:
    name: radarr-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
