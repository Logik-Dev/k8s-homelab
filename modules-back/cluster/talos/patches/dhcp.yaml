---
machine:
  network:
    interfaces:
      - interface: eth0
        dhcp: true
        dhcpOptions:
          routeMetric: 100
      - interface: eth1
        dhcp: false
      - interface: eth2
        dhcp: false
