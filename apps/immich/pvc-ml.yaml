apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-ml-longhorn
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 80Gi
