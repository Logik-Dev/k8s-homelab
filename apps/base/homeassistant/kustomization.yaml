apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - tailscale.yaml
commonLabels:
  app: homeassistant
configMapGenerator:
  - name: hass-config
    namespace: homeassistant
    files:
      - configuration.yaml
