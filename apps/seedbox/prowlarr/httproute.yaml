apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr
  namespace: seedbox
spec:
  parentRefs:
  - name: vlan200-gateway
    namespace: networking
  hostnames:
  - prowlarr.logikdev.fr
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: prowlarr
      port: 80
