apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
  namespace: seedbox
spec:
  parentRefs:
  - name: vlan200-gateway
    namespace: networking
  hostnames:
  - sonarr.logikdev.fr
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: sonarr
      port: 80