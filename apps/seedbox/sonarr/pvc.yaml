apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-longhorn
  namespace: seedbox
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
